[build]
  command = "npm run build"
  publish = "dist"

# React Router: evita 404 ao abrir /categoria/pizzas direto ou atualizar a pagina
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
