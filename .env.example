# URL da API (desenvolvimento)
VITE_API_URL=http://localhost:3001

# Producao no VPS (mesma origem): deixe vazio ou omita
# VITE_API_URL=

# Login admin e garcom: somente em backend/.env (ADMIN_* e WAITER_*)
# Nao coloque senhas no frontend (VITE_ADMIN_* removido por seguranca).
